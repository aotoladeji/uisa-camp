{
  "name": "uisa-camp",
  "version": "1.0.0",
  "description": "UI Sports Academy 2026 Summer Camp — Full Stack Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -k -n server,client -c yellow,cyan \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm start --prefix client",
    "test:server": "npm test --prefix server",
    "build": "npm run build --prefix client",
    "release:build": "npm run build --prefix client && node scripts/prepare-release.mjs",
    "release:s3": "node scripts/deploy-s3.mjs",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
